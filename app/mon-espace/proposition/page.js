import PropositionClient from './PropositionClient';

export const metadata = {
  title: 'Votre proposition de devis — Myracoustic',
  robots: { index: false },
};

export default function PropositionPage() {
  return <PropositionClient />;
}
